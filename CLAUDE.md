# tokyo-map 專案筆記

## 專案背景
這個專案原本在另一台電腦(使用者名稱 `lizlee`)上用 Claude Code 開發,但當初開 session 時沒有指定專案資料夾,對話紀錄留在那台電腦本機,尚未取回。

程式碼本身已經部署到 GitHub Pages(https://lizkuuri1999.github.io/tokyo-map/),因此改從 GitHub repo(https://github.com/lizkuuri1999/tokyo-map)重新 clone 回來救回。目前 repo 只有一個 commit「Add files via upload」，只有一支 `index.html`，沒有其他歷史紀錄或說明文件——也就是說,當初跟 Claude 討論的過程與決策**沒有**保留下來,只有最終成品程式碼。

## 多裝置協作慣例(重要)

這個專案會在兩台使用者名稱不同的電腦上開發(這台是 `liz`,另一台是 `lizlee`)。因為 Claude Code 的 session 索引是綁定專案的絕對路徑,兩台電腦使用者路徑不同會對不起來,所以統一規定:

- **兩台電腦都必須把這個專案放在同一個絕對路徑**:`/Users/Shared/Projects/tokyo-map`(不要放在 `~/Desktop` 或其他跟使用者帳號綁定的路徑)。
- Claude Code 的**對話紀錄本身不會跨裝置同步**,只有 git 追蹤的程式碼會同步。

### 切換電腦時的固定流程

1. 離開一台電腦前:`git add -A && git commit -m "..." && git push`
2. 到另一台電腦開始前:先 `git pull` 再動手
3. 不要在兩台電腦上同時修改,避免 merge 衝突

## 給未來 Claude session 的提醒
- 如果使用者要求接續「東京地圖」的開發,先確認目前 `index.html` 的實際內容,不要假設之前討論過的功能/設計已經實作或記錄在案——很可能沒有。
- 之後每次做出重要的設計決策或功能規劃,建議記錄回本檔案,維持跨裝置、跨 session 的上下文延續性。

## 專案範疇:已擴大成多頁旅遊規劃網站

這個專案已經從單純的「東京地圖」擴大成一個完整的旅遊規劃網站,規劃了 5 個頁面(依開發順序):

1. **`index.html`(首頁總覽)**——已完成。左側 sidebar 導航、中間是原本的東京互動地圖(可點擊熱區看景點/餐廳/咖啡廳/商店街資訊、自動連動地鐵路線疊圖)、右側是「行程籌備進度」待辦清單面板(目前用 `localStorage` 存在單一裝置,尚未跨裝置/跨旅伴同步)。900px 以下版面會收合成手機版抽屜選單。
2. **`airport.html`(機場交通)**——只有預留頁面。之後要做:機場到市內主要車站的交通比較,並支援輸入飯店地址/地標算出交通方式、時間、費用。
3. **`collect.html`(通通丟進來)**——只有預留頁面。之後要做:跟旅伴共同編輯的收集清單(景點/餐廳/購物),可一鍵從清單挑選項目排入行程。
4. **`itinerary.html`(行程安排)**——只有預留頁面。之後要做:可跟旅伴共同編輯、拖曳調整順序的行程表,並記錄修改者。
5. **`budget.html`(記帳分帳)**——只有預留頁面。之後要做:多人記帳、一鍵結算算出誰該付誰多少錢。

### 重要架構決策(尚未拍板,做到第 3 頁前務必先討論)

第 2~5 頁裡,**第 3、4、5 頁明確需要「跟旅伴共同編輯」**——也就是多人共用、即時同步的資料儲存。目前整個專案是純靜態網頁(GitHub Pages 部署,沒有後端、沒有資料庫),static 網站本身無法做到多人即時同步。開始動工第 3 頁「通通丟進來」之前,必須先跟使用者討論並決定共用資料的存放方案(例如 Firebase/Supabase 之類的免費 BaaS,或其他方案),這會影響第 3、4、5 頁的技術選型,不要自己假設或跳過這個討論直接動手。

### 共用檔案

- `shared.css`:sidebar / app-shell / 手機版抽屜選單 / 預留頁面卡片樣式,4 個預留頁面都靠這個檔案取得跟首頁一致的外觀。
- `sidebar.js`:手機版 sidebar 收合/展開的互動邏輯,4 個預留頁面共用。
- `index.html` 本身**沒有**改用這兩個共用檔案,樣式/邏輯是直接內嵌在檔案裡(避免修改已經測試過、能動的首頁時引入風險)。之後如果要把首頁也改成引用共用檔案,記得測試過再動手,別假設兩邊的 CSS/JS 完全等價。
