# tokyo-map 專案筆記

## 專案背景
這個專案原本在另一台電腦(使用者名稱 `lizlee`)上用 Claude Code 開發,但當初開 session 時沒有指定專案資料夾,對話紀錄留在那台電腦本機,尚未取回。

程式碼本身已經部署到 GitHub Pages(https://lizkuuri1999.github.io/tokyo-map/),因此改從 GitHub repo(https://github.com/lizkuuri1999/tokyo-map)重新 clone 回來救回。目前 repo 只有一個 commit「Add files via upload」，只有一支 `index.html`，沒有其他歷史紀錄或說明文件——也就是說,當初跟 Claude 討論的過程與決策**沒有**保留下來,只有最終成品程式碼。

## 多裝置協作慣例(重要)

這個專案會在兩台使用者名稱不同的電腦上開發(這台是 `liz`,另一台是 `lizlee`)。因為 Claude Code 的 session 索引是綁定專案的絕對路徑,兩台電腦使用者路徑不同會對不起來,所以統一規定:

- **兩台電腦都必須把這個專案放在同一個絕對路徑**:`/Users/Shared/Projects/tokyo-map`(不要放在 `~/Desktop` 或其他跟使用者帳號綁定的路徑)。
- Claude Code 的**對話紀錄本身不會跨裝置同步**,只有 git 追蹤的程式碼會同步。

### 切換電腦時的固定流程

1. 離開一台電腦前:`git add -A && git commit -m "..." && git push`
2. 到另一台電腦開始前:先 `git pull` 再動手
3. 不要在兩台電腦上同時修改,避免 merge 衝突

## 給未來 Claude session 的提醒
- 如果使用者要求接續「東京地圖」的開發,先確認目前 `index.html` 的實際內容,不要假設之前討論過的功能/設計已經實作或記錄在案——很可能沒有。
- 之後每次做出重要的設計決策或功能規劃,建議記錄回本檔案,維持跨裝置、跨 session 的上下文延續性。

## 專案範疇:已擴大成多頁旅遊規劃網站

這個專案已經從單純的「東京地圖」擴大成一個完整的旅遊規劃網站,目前規劃了 6 個頁面(依 sidebar 排列順序):

1. **`index.html`(首頁總覽)**——已完成。**不含地圖**,是純粹的標題頁:「東京旅遊🇯🇵」標題、旅行日期(12/23～12/27)、可點擊展開/收合的進度條(進度 100% 時文字變成「出發～🥳」),點擊後底下展開「行程籌備進度」待辦清單(目前用 `localStorage` 存在單一裝置,尚未跨裝置/跨旅伴同步)。手機版版面已知還有調整空間,使用者說先擱著,等後面幾頁功能做完再一次處理,不要自己主動去動。
2. **`airport.html`(機場交通)**——已完成(**不含地圖**,地圖已經獨立成 `spots.html`,見下)。中間是一個搜尋框,輸入飯店/車站/地標名稱後:
   - 用免費的 OpenStreetMap Nominatim API 做地址查詢(geocoding),把輸入轉成座標——**沒有用任何付費 API 或金鑰**,這是使用者明確選定的方案(見下方架構決策的討論)。
   - 用 Haversine 公式算出離哪個「熱門景點導覽」的熱區(11 個地區)最近,顯示**該地區**從成田機場出發的最快跟最省錢電車方案(時間、票價)。這些交通資料是**手動整理的參考值**,寫死在 `airport.html` 的 `AREAS` 陣列裡,不是即時查詢,精確度是「查到最近的熱區」而不是逐一飯店精算的門到門路線,頁面上有寫免責聲明。
   - 有「在地圖上查看」連結,會帶 `?area=<key>` 參數連到 `spots.html`,`spots.html` 會讀這個參數自動 `setActiveArea()` 並置中地圖(見該檔案 script 最後幾行)。
   - **踩過的坑**:一開始有把使用者輸入自動加上「東京」兩個字做查詢 bias,結果反而讓某些查詢(尤其含繁體「站」字,例如「澀谷站」)完全配對失敗,因為 OSM 資料庫用的是日文「駅」不是繁體「站」,多語系混合查詢在 Nominatim 的比對邏輯下有時會整個失敗而不是退而求其次。後來拿掉自動加字,改成:先用限制在東京範圍的 `viewbox` 查一次,查不到再放寬範圍查一次(`geocodeOnce(query, bounded)` 兩段式)。如果之後查詢配對率不好,先檢查是不是又不小心組合了奇怪的查詢字串。
3. **`spots.html`(熱門景點導覽)**——已完成,這是原本首頁/機場交通頁裡的東京互動地圖,獨立成自己的一頁。可點擊熱區看景點/餐廳/咖啡廳/商店街資訊、自動連動地鐵路線疊圖。這頁**不會**跟其他內容共用版位——使用者明確表示過地圖不要跟機場交通擠在同一頁。
4. **`collect.html`(通通丟進來)**——只有預留頁面。之後要做:跟旅伴共同編輯的收集清單(景點/餐廳/購物),可一鍵從清單挑選項目排入行程。
5. **`itinerary.html`(行程安排)**——只有預留頁面。之後要做:可跟旅伴共同編輯、拖曳調整順序的行程表,並記錄修改者。
6. **`budget.html`(記帳分帳)**——只有預留頁面。之後要做:多人記帳、一鍵結算算出誰該付誰多少錢。

### 重要架構決策(尚未拍板,做到「通通丟進來」前務必先討論)

第 4、5、6 頁明確需要「跟旅伴共同編輯」——也就是多人共用、即時同步的資料儲存。目前整個專案是純靜態網頁(GitHub Pages 部署,沒有後端、沒有資料庫),static 網站本身無法做到多人即時同步。開始動工「通通丟進來」之前,必須先跟使用者討論並決定共用資料的存放方案(例如 Firebase/Supabase 之類的免費 BaaS,或其他方案),這會影響後面幾頁的技術選型,不要自己假設或跳過這個討論直接動手。

### 機場交通頁的技術選型(已決定,別重新猜)

做「機場交通」時有先問過使用者要用哪種方案,選項是:(a) 免費地址查詢比對最近熱區(已採用)、(b) 串接 Google Maps API 做逐飯店精算(需要使用者自己申請金鑰、且金鑰會曝露在公開靜態網站原始碼裡有被盜用風險)、(c) 純固定清單文字比對不做地址查詢。使用者選了 (a)。以後如果要做「精確到任何地址」的路線規劃,那是之後才要重新討論的事,不要自己升級成方案 (b) 或改動 geocoding 邏輯的精神(比對最近熱區,不是逐一精算)。

### 共用檔案

- `shared.css`:CSS 變數、sidebar / app-shell / 手機版抽屜選單樣式,**所有頁面都共用這個檔案**。`spots.html` 額外有自己的 `<style>` 區塊放地圖專屬樣式(masthead/transit-panel/legend/info-panel等)以及地圖需要的額外 CSS 變數(`--c-shinjuku` 等地區色、`--line-yamanote` 等路線色、`--c-context`)——這些變數**只在 `spots.html` 裡定義**,不在 `shared.css`,如果之後有其他頁面也要用到地圖配色,記得補變數,不要假設 `shared.css` 已經包含。
- `sidebar.js`:手機版 sidebar 收合/展開的互動邏輯,所有頁面共用。
- 每新增一個頁面,記得同步把 sidebar 的導覽連結加到**所有現有頁面**裡(這是目前手動維護的,沒有共用的 nav 元件,容易漏改)。
- **重要 bug 教訓**:`shared.css` 的 `body` 一開始沒有設 `display:flex`,導致手機版 `.mobile-bar` 的高度沒有從主要內容區扣除,造成整頁多餘捲動。已修正為 `body{display:flex;flex-direction:column;height:100vh;overflow:hidden}` + `.app-shell{flex:1;min-height:0}`。如果之後新增頁面版面跑掉/多餘捲動,先檢查是不是又踩到類似的 flex 高度沒傳遞下去的問題。
